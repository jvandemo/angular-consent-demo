doctype html
html(ng-app="app")
    head
        meta(charset="utf-8")

        //- Display environment when not in production
        if environment === "production"
            title Angular Express
        else
            title= '[' + environment + '] - Angular Express'

        base(href="/")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")

        link(rel="stylesheet", type="text/css", href="app.css")

        //- Non production environment
        //- Load jspm loader and configuration
        if environment !== "production"
            script(src="_jspm_packages/system.js")
            script(src="config.js")
            //- Bootstrap the application
            script.
                System.import('app');

        //- Production environment
        //- Load self executing bundle
        if environment === "production"
            script(src="build.js")

    body

        header
            h1 Angular Consent
            h2 Easily add consent messages to your AngularJS application

        main

            section(consent="consent-one")
                section.ng-cloak(ng-show="$consent.hasNotAgreedYet()")
                    h1 Consent one
                    p This message will keep appearing until you click the Agree button. Even if you close your browser and come back later. Once you click "Agree" it will be remembered forever.
                    button(ng-click="$consent.agree()") Ok, agree
                section.ng-cloak(ng-show="$consent.hasAlreadyAgreed()")
                    h1 Thank you!
                    button(ng-click="$consent.reset()") Try again

            section(consent="consent-two")
                section.ng-cloak(ng-show="$consent.hasNotAgreedYet()")
                    h1 Consent two
                    p You can have multiple consents on a single page. The directive automatically keeps track of which consent is already agreed with.
                    button(ng-click="$consent.agree()") Ok, agree
                section.ng-cloak(ng-show="$consent.hasAlreadyAgreed()")
                    h1 Thank you!
                    button(ng-click="$consent.reset()") Try again

            section(consent="consent-three")
                section.ng-cloak(ng-show="$consent.hasNotAgreedYet()")
                    h1 Consent three
                    p You can use your own markup and CSS to make them fit perfectly in your application.
                    a(ng-click="$consent.agree()") Ok, agree
                section.ng-cloak(ng-show="$consent.hasAlreadyAgreed()")
                    h1 Thank you!
                    a(ng-click="$consent.reset()") Try again

        footer

            ul.links
                li: a(href="https://github.com/jvandemo/angular-consent", target="_blank") GitHub
                li: a(href="https://github.com/jvandemo/angular-consent/issues", target="_blank") Report an issue

            | Demo created by
            a(href="https://twitter.com/jvandemo", target="_blank") @jvandemo
            = ' using '
            a(href="http://www.angular-express.com", target="_blank") Angular Express
            = ' '
            | and hosted by
            = ' '
            a(href="http://surge.sh", target="_blank") Surge


